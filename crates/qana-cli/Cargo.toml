[package]
name = "qana-cli"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "The qana command: scaffold a language, certify a .qana grammar against the envelope, and inspect every layer it derives — tokens, tables, trees, services, incremental reparse, exports"
readme = "README.md"
keywords = ["cli", "parser", "grammar", "codegen", "lsp"]
categories = ["command-line-utilities", "development-tools"]

[[bin]]
name = "qana"
path = "src/main.rs"

[dependencies]
qana-lang.workspace = true
qana-grammar.workspace = true
qana-engine.workspace = true
qana-services.workspace = true
qana-sem.workspace = true

[package]
name = "qana-engine"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Incremental lexing and parsing over compiled grammars: line-anchored damage tracking, optimal tree reuse on edit, and error recovery that always yields a complete tree"
readme = "README.md"
keywords = ["incremental", "parser", "lexer", "editor", "reparse"]
categories = ["parsing", "development-tools"]

[dependencies]
qana-grammar.workspace = true

[dev-dependencies]
qana-lex.workspace = true

[[bin]]
name = "p1bench"
path = "src/bin/p1bench.rs"
